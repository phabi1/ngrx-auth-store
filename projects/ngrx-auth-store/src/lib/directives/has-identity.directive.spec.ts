import { HasIdentityDirective } from './has-identity.directive';

describe('HasIdentityDirective', () => {
  it('should create an instance', () => {
    const directive = new HasIdentityDirective();
    expect(directive).toBeTruthy();
  });
});
